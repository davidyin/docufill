{
  "name": "docufill-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create docufill-db",
    "db:migrate": "wrangler d1 execute docufill-db --file=src/db/schema.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241201.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
